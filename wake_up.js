// 主动消息功能已永久停用。
// 保留这个文件只是为了兼容 Railway 中可能存在的旧启动命令（node wake_up.js），
// 这样即便旧命令仍被执行，也不会定时唤醒模型、写入日记或发送 Bark / ntfy 推送。
console.log("wake_up.js 已停用：不会发送任何主动消息或推送。");

// 让进程保持存活，避免旧 Railway 双进程启动配置把“正常退出”误判成异常重启。
setInterval(() => {}, 24 * 60 * 60 * 1000);
